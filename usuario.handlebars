<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lista de Usuários</title>
</head>
<body>
    <h1>Usuários Cadastrados</h1>
    <table border="1">
        <tr>
            <th>Nome</th>
            <th>Email</th>
            <th>Senha</th>
            <th>Ações</th>
        </tr>
        {{#each usuarios}}
        <tr>
            <td>{{this.nome}}</td>
            <td>{{this.email}}</td>
            <td>{{this.senha}}</td>
            <td>
                <a href="/edit-usuario/{{this.id}}">Editar</a>
                <form action="/del-usuario/{{this.id}}" method="POST" style="display:inline;">
                    <button type="submit">Deletar</button>
                </form>
            </td>
        </tr>
        {{/each}}
    </table>
    <br>
    <a href="/usuario">Cadastrar Novo Usuário</a>
</body>
</html>
